import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignItems: "center",
  },
  elementContainer: {
    marginTop: 30,
    paddingTop: 50,
  },
});

export default styles;
